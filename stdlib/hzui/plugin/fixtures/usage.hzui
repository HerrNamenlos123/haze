import ui_components
import headwind
type presets = headwind.presets;

@props
title: str = "";

@setup
let save = () => {};

@template
div [col gap-2] {
    Button label="Save" @click=save {
        #content s {
            text [w-4] [s.label]
        }
    }
    text [] [props.title]
}
