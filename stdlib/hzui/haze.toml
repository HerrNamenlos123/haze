name = "hzui"
version = "0.0.1"
description = "SFC (single-file component) UI framework. Ships the SFC transformer as a compiler plugin (plugin/)."
license = "MIT"
authors = []
type = "lib"

[dependencies]
ui_components = { path = "../ui_components" }
ui_elements = { path = "../ui_elements" }
ui_styling = { path = "../ui_styling" }
headwind = { path = "../headwind" }
