
namespace builtin {
    extern C sin(v: f64): f64;
    extern C cos(v: f64): f64;
}

export sin(v: real): real {
    return builtin.sin(v);
}

export cos(v: real): real {
    return builtin.cos(v);
}
