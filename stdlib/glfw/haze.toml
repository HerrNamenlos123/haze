name = "glfw"
version = "1.0.0"
description = "GLFW support for Haze"
license = "MIT"
authors = ["HerrNamenlos123"]
type = "lib"
# src = "."

[scripts]
prebuild = "./build-linux.sh"

[linker]
flags = ["-lm", "-lglfw3", "-lGL", "-lpthread"]
