
@props
isActive: bool = true;

@slot
tab: {
    key: int;
}

@template [relative col w-grow h-grow]
// for tab in props.tabs {
    // let isActive = tab.key == props.activeKey;
    // div if=[props.isActive] [w-grow h-grow col] {
    //     slot tab key=[tab.key] {}
    // }
// }