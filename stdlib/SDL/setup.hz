


main(): i32 {
    const cwd = Process.cwd();
    if cwd.isSuccess {
        ctx.log.info("%s", cwd.success.cStr());
    }
    else {
        ctx.log.error("%s", cwd.error.cStr());
    }
    return 0;
}