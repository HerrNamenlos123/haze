
struct IntegerNode {

}

struct IdentifierNode {

}

array() {
    const staticArray = [3]u8{ 1, 2, 3 };
    const dynamicArray = []u8{ 1, 2, 3 };
    // const c = IntegerNode {};
    // const d = inline IntegerNode {};
    // const b = []u8 {};
    // const a: []u8 = []u8 {1, 2, 3};
    // a.push(4);
    for (let i = 0; i < staticArray.length; i++) {
        fmt.println("Element: ", staticArray[i]);
    }

    staticArray[1] = 3;
    dynamicArray[1] = 5;

    // dynamicArray.push(42);

    for (let i = 0; i < dynamicArray.length; i++) {
        fmt.println("Element: ", dynamicArray[i]);
    }
}

extern C parse(source: str) {
    const result = fs.read(source);
    if result {
        fmt.println("Content: ", result);
    }
    else {
        fmt.println("Failed");
    }

    array();
    // const arr = array();
}

y(d: int) :: final {
    x (d + 1);
}

x(d: int) :: final {
    y(d + 1);
}

a() {
    x(0);
}

bar() :: final {
    const c = 1;
    // const r = 1;
    // const e = 1;
    // const w = 1;
    // const q = 1;
    // const i = 1;
    // const s = 1;
    // a();
    // __c__("int* a = 0; int b = *a;");
    // test();
}

test() {
    const a = 0;
    bar();
}

main() {
    type LexerNode = IntegerNode | IdentifierNode;
    test();
    parse("stdlib/core/src/String.hz");
    return 0;
}