
import math
import glfw
import gl
import wgpu
import glfw_wgpu

extern C const WGPURequestAdapterStatus_Success: wgpu.ffi.WGPURequestAdapterStatus;

let g_adapter = none as wgpu.ffi.WGPUAdapterImpl | none;
let g_adapterReady = false;

adapterRequestCallback(status: wgpu.ffi.WGPURequestAdapterStatus,
                                      adapter: wgpu.ffi.WGPUAdapterImpl,
                                     message: inline wgpu.ffi.WGPUStringView,
                                     userdata1: cptr,
                                     userdata2: cptr): void {
  if (status == WGPURequestAdapterStatus_Success) {
    g_adapter = adapter;
    g_adapterReady = true;
    fmt.println("Adapter request successful");
  }
  else {
    g_adapterReady = true;
    fmt.println("Adapter request failed");
  }
}

get() {
    // return 1;
    return 3;
}

test() {
    // let a = r"foo\/bar[i]*"gim;
    // let b = r"beeeebb[i]*"gim;
    // let c = r"foo\/bar[i]*"mig;

    let r = r"\w+";
    let text = "hello"

    attempt {
        fmt.println(regex.match(regex.runtime("\\w+", "")?!, text))
    }
    else e {
        fmt.println("Regex Error:", e)
    }

    let r1 = r"world"
    let r2 = r"^world$"

    fmt.println(regex.match(r1, "hello world"))
    fmt.println(regex.match(r2, "hello world"))
}

main(): int {

    test_regex();
    // integer_comparison_runtime_tests();
    // test();
    return 0;

    let a = attempt {
        let v = get();
        if v == 1 {
            raise "Oh no it's 1";
        } 
        else if v == 2 {
            raise 187;
        }
    }
    else e {
        if e is str {
            fmt.println("Error: ", e);
        }
        else {
            fmt.println("Error: ", e);
        }
    }

    return 0;
    // let a = fs.exists("my/very/test");
    // if !a {
    //     fmt.println("exists", a);
    // }
    // else {
    //     fmt.println("RESULT", a);
    // }
    // return 0;

    if !glfw.init() {
        return 0;
    }

    const window = glfw.createWindow({ 
        title: "WebGPU Example",
        noApi: true,
    });
    if !window {
        fmt.println("Window creation failed");
        return 0;
    }
    // wgpu.init(window.handle);

    let instance = wgpu.createInstance();
    if !instance {
        fmt.println("WGPU instance could not be created");
        return 0;
    }

    let surface = glfw_wgpu.createWindowWGPUSurface(instance, window);
    if !surface {
        fmt.println("Failed to create WGPU GLFW surface");
        return 0;
    }

    let adapterOpts = wgpu.ffi.WGPURequestAdapterOptions {
        compatibleSurface: surface.handle,
    };
    let adapterCallback = inline wgpu.ffi.WGPURequestAdapterCallbackInfo {
        callback: adapterRequestCallback,
    };
    instance.requestAdapter(adapterOpts, adapterCallback);

    let timeout = 1000;
    while !g_adapterReady && timeout > 0 {
        for (let i = 0; i < 10000; i++) {
        }
        timeout--;
    }

    if (!g_adapter) {
        fmt.println("Failed to get adapter after timeout");
        return 0;
    }

//   WGPUDeviceDescriptor device_desc = { 0 };
//   WGPURequestDeviceCallbackInfo device_callback = {
//     callback = device_request_callback,
//   };
//   wgpuAdapterRequestDevice(g_adapter, &device_desc, device_callback);

//   // Wait for device to be ready
//   int timeout = 1000; // 1 second timeout
//   while (!g_device_ready && timeout > 0) {
//     // Simple busy loop with minimal sleep
//     for (int i = 0; i < 10000; i++) {
//       // Spin
//     }
//     timeout--;
//   }

//   if (!g_device) {
//     fprintf(stderr, "Failed to get device after timeout\n");
//     return;
//   }

    // gl.init(window.getProcAddress());
    // gl.viewport(0, 0, 1920, 1080);

    while !window.shouldClose() {
        glfw.pollEvents();
        // wgpu.wgpu_start(window.handle);

        // gl.clearColor(Color((math.sin(glfw.getTime() * 10.0) + 1.0) / 2.0, 0.1, 0.1, 1.0));
        // gl.clear();
        // wgpu.wgpu_end(window.handle);
        // window.swapBuffers();
    }

    window.destroy();
    glfw.terminate();

    return 0;

    // fmt.println("TODO: Implement a proper platform configuration system. Currently there is an interface.compiler.defines feature to define transitive macros, but this is not the solution, it is only a workaround. We rather want a real, proper configuration system where we can e.g. differentiate between x11 and wayland platform configurations and compile for one of them.");

//     fmt.println("TODO: Fix returns, if function returns in one path but not all, it must either create an error or automatically return none");
//     fmt.println("Idea for later: (from Prime Reacts: The Flaws of Inheritance): I should implement interfaces as a way to make a contract about generic types, and to make the interfaces very powerful, interfaces should be able to define members and methods, as well as default implementations of methods that is used if the struct does not provide that function, and it is allowed as long as the default implementation of the interface method only uses other methods of the interface. This makes it very powerful and a complete replacement for polymorphism.");
//     fmt.println("Idea: Implement scopes/do blocks/whatever, and like in Swift make the ability to label them, and then 'break foo' can be used to break out of that scope, which is awesome because it greatly simplifies the common issue of having 2 nested loops and wanting to break out of the outer one.")

//     if !glfw.init() {
//         return 0;
//     }

//     const window = glfw.createWindow({ 
//         title: "Hello World!",
//     });

//     if window {
//         window.swapInterval(0);
//         gl.init(glfw.getProcAddress());

//         gl.viewport(0, 0, 1920, 1080);

//         let oldTime = glfw.getTime();
//         window.swapBuffers();

//         let frameCounter: i16 = 0;

//         let fps = 0.0;
//         while !window.shouldClose() {
//             glfw.pollEvents();

//             let now = glfw.getTime();
//             let elapsed = now - oldTime;
//             oldTime = now;

//             gl.clearColor(Color((math.sin(oldTime * 10.0) + 1.0) / 2.0, 0.1, 0.1, 1.0));
//             gl.clear();

//             if fps == 0.0 {
//                 fps = 1.0 / elapsed;
//             }

//             let alpha = 0.001;
//             fps = fps * (1.0 - alpha) + 1.0 / elapsed * alpha;

//             fmt.println("FPS:", fps);
//             fmt.println("Count: ", frameCounter);

//             window.swapBuffers();
//         }

//         glfw.terminate();
//     }

//     return 0;
}
